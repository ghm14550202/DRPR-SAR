# Optional pretrained-model mount point

Place optional local pretrained files here when a selected legacy model
requires them. The ResNet helpers accept `RESNET50_WEIGHTS`; no credential or
machine-specific path is stored in this bundle.
